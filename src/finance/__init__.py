"""Finance vertical — the first domain of the Life OS.

Transactions are STRUCTURED data: parsed, categorized, and computed exactly
(never embedded or retrieved by similarity). The RAG/vector layer is reserved
for the soft layer (goals, notes, reflections) added later. This split is the
"hybrid memory" principle: exact math on facts, semantic recall on text.
"""
